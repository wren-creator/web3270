# RACF PROBE — default-credential wordlist ("Load list" button)
#
# One credential per line, "userid:pass" or "userid,pass".
# Blank lines and lines starting with # are ignored.
#
# This file is a starting point. Replace it with the list curated for the
# lab system you are authorized to test. It is read live (bind-mounted in
# Docker via DEFAULT_ACCOUNTS_FILE), so edits take effect without a rebuild.
#
# The RACF PROBE "Load defaults" button carries the built-in per-subsystem
# lists (TSO / z/VM / CICS / z/TPF / IBM i) if you would rather use those.

# ── z/TPF operator logon ──
TPFOP01:TPF1
SYSOP01:SYS1
ADMIN01:ADMIN
PRIME:PRIME
CRAS:CRAS
OPER:OPER
TPFOPER:TPFOPER
SYSOP:SYSOP

# ── IBM i (AS/400) Sign On ──
QSECOFR:QSECOFR
QSRV:QSRV
QUSER:QUSER
QPGMR:QPGMR
QSYSOPR:QSYSOPR
QSECADM:QSECADM
QSYS:QSYS
