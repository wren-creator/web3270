# Placeholder only — do NOT commit real values to git.
# Create the real secret out-of-band instead, e.g.:
#   oc create secret generic tn3270-bridge-secret \
#     --from-literal=SECURITY_TOOLS_PASSWORD='<real-password>'
apiVersion: v1
kind: Secret
metadata:
  name: tn3270-bridge-secret
  labels:
    app: tn3270-bridge
type: Opaque
stringData:
  SECURITY_TOOLS_PASSWORD: "CHANGEME"
