# Local configuration — never commit
.env
node_modules/

# User macros — local data, never commit
macros/local/
macros/accounts/

# Private connection profiles — user's own hosts, never commit
ssh-hosts.txt
lpars.txt

# RACF PROBE wordlist — operator's own default-credential list, edited in place
default-accounts.txt

# Stale root-level artifacts
macros.json
macros/macros.json

# Working doc, not for the repo
NDA.md
