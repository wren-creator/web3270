# Roadmap

## Graphics
- [x] Arcs — `tn3270/gddm.js` now decodes Set Arc Parameters (X'22'), three-point Arc (X'C6'), and Full Arc (X'C7') per the GDDM Base Programming Reference (Appendix D), and `public/js/gddm.js` renders circles/ellipses via a canvas transform + circumcircle geometry for the three-point form. `mock-lpar.js`'s `GDDM` demo now exercises both. The "at current position" short forms (X'86'/X'87') are intentionally skipped, same as the existing GCHST-at-current-position handling — this decoder doesn't track current position across orders.
- [x] Fillets — `tn3270/gddm.js` now decodes the Fillet order (X'C5', a polyfillet of 2+ points). `public/js/gddm.js` renders it as a series of quadratic Bezier pieces through consecutive segment midpoints, which exactly satisfies the manual's tangency rule (curve tangent to the first/last line at their endpoints, tangent to intermediate lines at their midpoints) and its two special cases (2 points → straight line, 3 points → a single smooth arc). `mock-lpar.js`'s `GDDM` demo now draws a 5-point decorative ribbon to exercise the multi-piece case.
- [x] Character sets — `tn3270/gddm.js` now decodes the Character Set order (X'38'). Per the 3270 Data Stream Programmer's Reference (GA23-0059), the Object Data structured field's OBJTYP has no value for transmitting symbol-set glyph data at all (only Graphics X'00' and Image X'01' exist), so a symbol set is necessarily a client-resident resource, like a built-in font, not something sent over the wire — the decoder just passes the selected LCID through with the character string. `public/js/gddm.js` owns the actual glyph table (`VECTOR_SYMBOL_SETS`), currently one hand-authored 7-segment-style digit set (LCID X'41'), rendered as independent line strokes at a fixed pixel size next to regular labels. `mock-lpar.js`'s `GDDM` demo now shows a "TOTAL" readout whose digits render through this custom font, visibly distinct from the default browser-font text everywhere else on the same screen.
- [x] Images — `tn3270/gddm.js` now decodes the Begin Image (X'D1', FORMAT 0 only) / Image Data (X'92') / End Image (X'93') sequence: a monochrome bitmap, 1 bit per display point, one row per Image Data order. Verified byte-for-byte against the manual's own worked example (a 9x4 hollow square). `public/js/gddm.js` rasterizes the packed bits onto an offscreen canvas in the current color, then scales it into place with image smoothing off for a crisp pixel-art look. The manual doesn't state row order or which corner the position anchors — this renderer's documented, reasoned interpretation is top-to-bottom rows anchored at the bottom-left corner (ordinary raster convention). `mock-lpar.js`'s `GDDM` demo now draws a hand-encoded 8x8 up-arrow icon, scaled up in the corner. **Correction:** every image primitive was actually invisible in the browser until now. Each row was stored as a Node `Buffer`, which crosses a `JSON.stringify` boundary (`utils/send.js`) on its way to the client; `Buffer` serializes there as `{type:'Buffer',data:[...]}`, silently breaking the renderer's `row[c>>3]` indexing (every lookup came back `undefined`, so no pixel ever got painted). Fixed by storing rows as plain arrays in the decoder. Verified in-browser (Playwright against a rebuilt Docker stack): the up-arrow icon now renders with real opaque pixels.
- [x] Color-mix modes — `tn3270/gddm.js` now decodes the Color Mix order (X'0C', short format), the AS/400 GDDM Reference's Appendix B "Color Mix Order" (GSMIX): default, mix (OR), overpaint, underpaint, exclusive OR. `public/js/gddm.js` maps each mode to the closest real `globalCompositeOperation` — overpaint is ordinary drawing, underpaint maps exactly to `destination-over` (old color wins where both are set), xor has a direct canvas match, and mix (OR) uses `lighter` as the nearest analog since canvas has no literal OR blend. `mock-lpar.js`'s `GDDM` demo now draws four pairs of overlapping filled squares, one pair per mode, below the chart's x-axis (verified rendering correctly distinct per mode via Playwright screenshots against a rebuilt Docker stack). Clipping is intentionally NOT implemented: the manual's Appendix B GDF order list is alphabetical and has no Clip order at all (nothing sorts between "Character Shear" and "Color" where one would land) — `GSCLP` is a host-side GDDM API call that shapes what the mainframe puts in the GDF stream before it's ever sent, so there's nothing on the wire for a terminal-side decoder to act on. This closes out full GDF order-set coverage for the non-segment, non-shading-pattern orders.
- [x] Hidden `USA` easter egg — a WarGames/WOPR nod. Typing `USA` at the TSO READY prompt (or from the TSO command shell) brings up a "GLOBAL THERMONUCLEAR WAR" screen: a stylized continental-US outline (Line order) and a fan of curved trajectory arcs (Arc order) radiating from a central launch-site marker out past the picture boundary, all built entirely from the GDF orders this engine now supports. Same TSO-command-triggered pattern as the `GDDM` demo (`mock-lpar.js`'s `screenUSA`/`buildUsaObjectDataWsf`).

## 3270 Datastream
Issues flagged on web3270#19 (the field color/highlight propagation bug, fixed).
- [x] ISPF partial-update screen corruption — web3270#19, reported by racingmars. ISPF rarely erases between panels, it relies on partial writes that reuse buffer addresses. `_processWriteCommand`'s plain-character and RA (Repeat to Address) branches mutated the existing buffer cell in place and only ever touched `.char`, so a stale `.fa` (and its stashed SFE color/highlight) from an earlier field attribute byte at that address stuck around forever once the host reused the position for plain content. `_bufferToRows`/`_extractFields` both key off `cell.fa !== undefined` to find field boundaries, so every stale cell became a phantom field, splitting real fields and corrupting the color/highlight forward pass at the wrong spot, exactly matching his screenshots and the field desync he saw when typing. Verified against his own captured wire log (login → ISPF → "3.4" DSList panel): before the fix, that panel's partial write left 1,541 cells with stale `.fa` and `_extractFields()` returned 260 fields (204 phantom); after, 0 stale cells and 149 fields, exactly matching the 149 real field-attribute bytes, with the rendered panel matching real ISPF line for line. Fixed in `fix/pipe-render-font`; both branches now clear `.fa`/color/highlight unconditionally on write. Racingmars's own suggested fix (a single forward pass recomputing display attributes at EOR) was already in place from the earlier color-propagation fix — this bug was upstream of that, in the buffer mutation itself.
- [ ] Graphic Escape / codepage 310 support — `tn3270/session.js`'s `ORDER_GE` handler just skips the next byte (`i += 2`) instead of decoding it. Root cause confirmed for why go3270's example1 status bar (green/yellow/red graphics characters) never renders. Needs a codepage 310 (graphics/symbol) table plus real GE decoding in the datastream parser.
- [ ] Vertical pipe `|` not rendering — web3270#21. Data path proven clean: replayed the reporter's own captured wire log through the real `_handle3270Record`/`_bufferToRows` code and every EBCDIC `0x4F` decodes to `|` correctly; traced the rest of the pipeline (emit → WS JSON → `renderLiveScreen()`) by hand with nothing found. Leading theory is the CDN-loaded IBM Plex Mono/Sans (`fonts.googleapis.com`) silently failing on the reporter's network-restricted VM and falling back to a bare `monospace` keyword — both his browsers (Firefox, Chromium) showed identical behavior, consistent with a shared substituted font. `fix/pipe-render-font` self-hosts the fonts and drops the CDN dependency entirely; verified via Playwright that pipes render correctly against the self-hosted font, but unconfirmed against the reporter's actual failure — waiting on him to test the branch or supply devtools confirmation.

## Theming
- [x] Hidden "Barbie" theme easter egg — secret click on the topbar logo flips a full pink/gold palette (terminal + chrome + logo icon), persisted in localStorage. No visible affordance by design.
- [ ] Company/corporate theme support — per-deployment logo + brand colors. Should extend the existing visible theme system (`public/js/settings.js` `THEMES` object + `.theme-swatches` UI in `tn3270-client.html`) rather than build a new mechanism: add a named entry + a swatch, same as `green`/`blue`/`amber`/`white`/`teal`. Needs a swappable logo asset (the topbar SVG logo colors were decoupled to CSS vars — `--logo-bg`/`--logo-ring`/`--logo-ring-light` in `terminal.css` — as groundwork for this).

## Session Profiles & Access Control
Feedback from the 2026-07-23 company meeting, ahead of opening the tool up beyond the original one-group user base.
- [x] Rename "LPAR PROFILES" to "session profiles" throughout the project. Matches the term users actually recognize; "LPAR" is mainframe-partition jargon that doesn't map to what the feature does for most users. Renamed UI text, JS state/identifiers, and CSS classes across `config.js`, `public/tn3270-client.html`, `public/js/walkthrough.js`, `public/js/profiles.js`, `public/js/xfer.js`, `public/js/state.js`, and `public/css/terminal.css`. Left `lpars.txt`/`lpars.shipped.txt` and `loadLparFile()`/`parseLparFile()` unchanged so existing deployments don't need a migration step.
- [ ] Private macro library — as the user base grows past the original single group, some users need macros that aren't shared/visible to the whole group.
- [ ] Role-based access control, gated on a group-admin role, covering:
  - Session profile management (view/edit) and macro edit/delete permissions.
  - **Developer/maintainer** — full access to everything.
  - **Security** — access to security tools, including entering the security-tools password.
  - **Customer** — basic views (scope still TBD), can create personal macros but cannot edit or delete other users' macros.
  - **System-admin** — no access to security tools, but the password field/prompt is still present in their view.
- [ ] VBScript-to-macro converter — a tool to convert an existing VBScript macro into the macro library's JSON format (see `state.macros` step/name/description shape in `public/js/macros.js`). Lets users bring macros over from legacy TN3270 emulators instead of rebuilding them by hand.

## Hosted Paid-Tier Product (webterm-3270.com)
Extends the "opening the tool up beyond the original one-group user base" push above into a commercial, self-serve direction: three paid tiers (base/training/full) with PayPal billing, gated behind `BRIDGE_MULTI_TENANT` so the internal/OpenShift deployment is completely unaffected. Domain `webterm-3270.com`.
- [x] Postgres accounts/billing schema (`db/schema.sql`) — `accounts`, `profiles` (sku/review_status), `pending_signups`, `verification_codes`, `disclaimer_acceptances`, `session_profiles`, `ssh_host_profiles`.
- [x] Signup + phone-OTP verification (Telnyx SMS) and login/session cookies + post-login email verification (SMTP).
- [x] PayPal subscription billing (`billing/paypal.js`, `routes/billing.js`) — checkout, return-url reconciliation, and a real webhook receiver (unlike the internal reference implementation this was modeled on, which only reconciles on return-url and misses async status changes like renewal failures).
- [x] Full-tier disclaimer gate + admin reach-out notification (`routes/disclaimer.js`) — full tier (security/recon/fuzzing tools against real hosts) isn't pure self-serve; checkout 403s until the current disclaimer version is accepted, and accepting it emails an admin to review the account.
- [x] Themed login/signup (`public/login.html`) and billing/checkout (`public/billing.html`) pages, styled off `terminal.css` (the project's existing CSS source of truth) rather than a separate look.
- [x] `handlers/ws.js` tier gating — a `SKU_CAPS` map (base: client only, training: + shipped mock LPARs, full: + security tools) enforced on the WS connect handshake.
- [x] Per-account data isolation — session/SSH profiles, traffic log + pcap captures, recordings, and macros were all single shared file/in-memory structures with little to no auth check; scoped per account (`auth/session-owners.js`, `db/session-profiles.js`, `db/ssh-host-profiles.js`, `utils/account-paths.js`) so hosted customers can't see each other's connections, live screen traffic, or saved macros. Server logs (`routes/logs.js`) went admin-only instead, since they aren't scopeable per-account.
- [x] Anonymous visitors on `/` redirect to `/login` on the hosted deployment (`public/js/auth-gate.js`, `GET /api/config`) — a no-op on the internal deployment.
- [x] Render deploy config (`render.yaml`, `docs/render-deploy.md`) — bridge + managed Postgres + the five mock LPARs as private services, reusing the existing Dockerfile.
- [x] `db/create-admin.js` (`npm run admin:create`) — CLI tool to create/promote an admin account (`is_admin`, pre-approved full tier), since nothing set that column before.

## Dataset Export
- [ ] Excel (.xlsx) export for dataset search/recon results, alongside the existing CSV export (`reconExportCsv()` in `public/js/recon.js`). Covers both the Dataset Recon Scanner output and general dataset search, so results can be handed off in the format most teams actually open.

## Background Sessions
- [ ] Detached/background sessions — let a TN3270/TN5250 session keep running server-side after the browser tab that started it closes, instead of tearing down when the WebSocket drops (today a session's lifetime is tied 1:1 to its browser WebSocket — see the `sessions` Map in `server.js` and `Tn3270Session` in `tn3270/session.js`). Useful for long-running unattended work (batch monitoring, recon scans) that shouldn't die just because someone closed a laptop lid.
- [ ] Status dashboard popup listing all running sessions (host, profile, connected duration, state) with a kill action per session, so an orphaned/background session can be terminated without restarting the whole bridge.

## Mock AS/400 / RPG
- [x] Real RPG interpreter for `mock-as400.js` — `WRKMBRPDM` option `4=Run` (or `CALL PGM(lib/name)`) on `APPLIB/ADVENTURE` now runs a genuine, if scoped-down, RPG IV interpreter (`mock-lpar/rpg/{dds,rpgle,interpreter,expr}.js`) against real fixed-form RPGLE + DDS source (`mock-lpar/rpg/programs/adventure.{rpgle,dspf}`), not canned output — the game is a small text-adventure (explore, fight/flee goblins, win/lose, F3 anytime). Column positions for both spec types were pulled from IBM's own ILE RPG Language Reference (SC09-2508) and the DDS Reference: Display Files manual via `pdftotext`, not reconstructed from memory, matching this project's existing byte-level-verification convention for the 5250 datastream itself. Architecture note: `EXFMT` (write a format, then read it) maps onto a JS generator that yields the format to render and resumes via `.next({key, values})` on the next AID key, which is what lets a running program's state live entirely outside the daemon's `screen` state machine as just one more screen value (`RPG_RUN`). Supported opcode subset: `EVAL`, `IF/ELSE/ENDIF`, `DOW/ENDDO/LEAVE`, `SELECT/WHEN/OTHER/ENDSL`, `EXSR/BEGSR/ENDSR`, `EXFMT`, `DIV/MVR`, `RETURN`. Random rolls deliberately use classic `DIV`/`MVR` for modulo rather than a compound `EVAL` expression, since RPG has no `%REM` until well past V4R3 (the real hardware target for this feature) and intermediate-expression truncation inside one `EVAL` isn't unambiguous real RPG behavior. Caught and fixed a real bug along the way: the expression evaluator's `AND`/`OR` handling used JS's short-circuiting `&&`/`||`, which skipped *parsing* (not just evaluating) the right-hand operand whenever the left side was already decisive, leaving unconsumed tokens and throwing — now both sides are always parsed regardless of the short-circuit value. See `mock-lpar/README.md`'s "A real RPG interpreter" section for the full architecture writeup.

## Mock z/OS / JCL
- [x] Multi-member dataset browsing + real SUBMIT/SDSF pipeline for `mock-lpar.js` — built to give the z/OS mock the same real-content depth the AS/400 mock already had, ahead of scoping the Mainframe 102 follow-on book for z/OS. `DEMO.JCL.CNTL` in ISPF 3.4 now drills into a real member list (`MYJOB`, `QTRRPT`, `BADJOB`), real fixed-form JCL on disk (`mock-lpar/jcl/programs/*.jcl`), parsed at startup for `//stepname EXEC PGM=x` lines and mapped to a canned RC/message via the `PGM_OUTCOMES` table, the same scoped-real approach as the RPG interpreter, not a full JCL language interpreter. `SUBMIT`/`SUB`/`S member` assigns a real job number; status (`QUEUED`→`ACTIVE`→`OUTPUT`) is computed off elapsed time since submission rather than a timer. SDSF gained a job list (`ST`/`DA` at the existing COMMAND INPUT line), seeded with job history plus `BADJOB` as a deliberate non-zero-RC (0012) example. Purely additive — the original ISPF option `M` → static `JOB07432`/`MYJOB` detail path is byte-for-byte unchanged, verified with a scripted protocol-level smoke test. Caught and fixed two real bugs along the way: a field-layout collision in the new ISPF 3.4 selection field that would have overwritten the next field's attribute byte, and a shared-`lastScreen`-variable clobber between the new screens and the pre-existing ispf34/sdsf navigation that would have broken PF3 "go back" on screens Book 1 already documents. See `mock-lpar/README.md`'s "Real JCL: dataset member browsing and SUBMIT" section.

## Mock z/VM & z/TPF — Mainframe 104 prep
Task list for the z/VM & z/TPF learning-series book (see the `books` repo's `ROADMAP.md` "Learning Series" section for the series-level decision — Book 4, "Specialized Engines: z/VM & z/TPF"). Content audit as of 2026-08-19: closer to book-ready than that entry's original "neither platform's mock content is deep enough yet" call suggested — most of the gap was already closed by `feat: add Mainframe 101 training content to the mock LPAR fleet` (the z/VM REXX interpreter) and the earlier z/TPF Wave 4 / ticketing work.

- [x] z/VM CP layer: LOGON, CP READY (`QUERY TIME/NAMES/STORAGE/DASD`, `LINK`, `HELP`, `LOGOFF`) — book-ready.
- [x] z/VM minidisk password exposure teaching moment (`vmminidisk.js` + `mock-zvm.js`'s `LINK` handling — a command-line argument has no way to be marked secret the way the LOGON password field is) — book-ready, same shape as Book 3's RACF-lockout chapter.
- [x] z/VM CMS layer: FILELIST, RDRLIST, XEDIT — book-ready.
- [x] z/VM CMS REXX exercises — added `MAXVAL` to `rexx/execs.js`: finds the largest of up to three passed numbers (sequential `IF` comparisons), then counts down from it to 1 via `DO`/`TO`/`BY -1`, flagging values 3 and under as `*** LOW ***` via `IF`/`THEN`/`ELSE` and `||` concatenation inside the loop. No separate wiring needed in `mock-zvm.js` — `CMS_EXECS` is the single source of truth XEDIT display and CMS command dispatch both key off, so adding the entry to `buildExecs()` was sufficient. Covered by two new tests in `rexx/interpreter.test.js` (full run, and the missing-args-default-to-0 path); `DOCUMENTATION/MOCK-SERVERS.md`'s REXX exec table updated to match. Book-ready.
- [x] z/TPF operator console: LOGON at 3 privilege levels, `ZSHOW E/P/S/T/O/V/B`, `ZTEST ENTRY`, `ZSTOP/ZENTRY/ZPROG/ZEND`, `HELP` — book-ready.
- [x] z/TPF PNR ticketing exercise: `ZBOOK`/`ZLOOK`/`ZCXL`/`ZSHOW B`, real 6-character record locators and `ZTPF8xx`-style messages — book-ready.

All items above are book-ready as of 2026-08-19 — no further mock-building expected before Book 4 drafting starts.

## Cross-Platform Token Chain — Mainframe 105 (Book 5) prep
Task list for Book 5, "Advanced/Capstone: cross-platform orchestration," the final title in the Mainframe 101-105 learning series. Unlike Books 2-4, there was no existing content gap to close per platform, the gap was a missing *mechanic*: nothing today makes a student's work on one mock platform a verified prerequisite for another, so a capstone built purely on existing exercises would be skippable (log on to whichever mock is last, type the answer straight from the book, done). Scoped 2026-08-20 as a four-hop "Data Integrity Night" token chain, a quarterly cross-system verification pass, chosen to lean into z/TPF's existing airline/PNR theme rather than invent a new domain: z/OS produces a Batch Control Number (BCN), IBM i validates it and issues a Resource Clearance Code (RCC), z/VM's REXX interpreter checks the RCC and computes a System Authorization Value (SAV), and z/TPF's `ZBOOK` requires both the BCN and SAV to finalize a booking. Each hop only needs to independently validate a fixed value baked into that mock's own source, no shared datastore or inter-process communication between mocks, same single-process-state discipline every mock in this fleet already follows. The actual hands-on mechanic is the client's existing multi-session tabs (open all four mocks in one browser window, already shipped, no client work needed), carrying each token from one tab's output into the next tab's input by hand.

- [x] z/OS: a new one-step job, `DATACHK` (`jcl/programs/datachk.jcl`, `PGM=DATACHK` registered in `PGM_OUTCOMES`), whose SDSF output issues the fixed Batch Control Number `BCN-7742`. Verified live: connected a real TN3270E client, logged on, `SUBMIT DATACHK`, navigated ISPF → M → ST → `S JOB0xxxx`, confirmed the BCN in the SDSF detail screen's `IEF285I` line.
- [x] IBM i: a new CL command, `CHKBCN BCN(bcn)` (`EXPECTED_BCN`/`ISSUED_RCC` constants + a case in `runCommand`'s switch in `mock-as400.js`), validates against the fixed BCN and on success issues the fixed Resource Clearance Code `RCC-4419`. Verified directly against `runCommand()` (TN5250 has no test client in this environment): missing BCN, correct BCN, and wrong BCN all produce the right message.
- [x] z/VM: a new REXX exec, `VERIFY` (`rexx/execs.js`'s `buildExecs()`, no separate wiring needed since `CMS_EXECS` is the single source of truth), takes the RCC via `PARSE ARG`, checks it with a plain `IF`/`ELSE` equality comparison, `SAY`s the fixed System Authorization Value `SAV-2081` on a match. Verified live over real TN3270E (z/VM's mock needs the *client* to initiate TN3270E device-type subnegotiation, the opposite of z/OS/z/TPF — see the test client notes) and covered by two new tests in `rexx/interpreter.test.js`.
- [x] z/TPF: `ZBOOK`'s signature extended to `ZBOOK passenger,flight,date,seat[,bcn,sav]` — the two new params are optional, omitted entirely `ZBOOK` books exactly as it always has (Books 1 and 4 both document and teach the plain 4-argument form; verified that path is still byte-for-byte unchanged). Supplied, both must match `BCN-7742`/`SAV-2081` or the booking is rejected with `ZTPF852E`/`ZTPF853E`. Verified live over real TN3270E: old 4-arg form still books, correct 6-arg form books, wrong BCN alone rejects, wrong BCN and SAV both reject with both messages.
- [x] Walked the whole chain start to finish against the real running mocks (a hand-rolled TN3270E test client for z/OS/z/VM/z/TPF, since the tn3270-bridge tool can't reach localhost in this sandbox, same issue noted in earlier commits; a direct `runCommand()` call for IBM i's TN5250 mock), not just read from source. `DOCUMENTATION/MOCK-SERVERS.md` and `mock-lpar/README.md` updated to document all four hops.

All four hops verified live and working, including the two-way backward-compatibility check on `ZBOOK`. Book 5 drafting can start. Likely structure to scope next: probably fewer, denser sessions than Books 2-4 since this is one integrated exercise rather than platform-by-platform coverage, plus a look back across the whole 101-104 series as the actual capstone-of-capstones.

## Operational Judgment — Mainframe 200 series prep

Series-level plan for the 200 series, the follow-on to the now-complete 101-105 learning series. Shift from "here's the command" to "here's the judgment call," troubleshooting/incident-response scenarios plus a light, non-overlapping security touch (heavier security depth reserved for a future 400 series). Full plan: `/Users/britleywrenhoff/.claude/plans/let-s-plan-out-what-lively-tarjan.md`.

Two constraints established before any book got scoped: `WebTerm/3270: The Nuts and Bolts Guide` already owns almost all the obvious security-tooling content (deep z/OS RACF recon, CICS/DB2, the z/VM minidisk exposure, and the entire IBM i 7-tool security suite), so none of that is available to reuse. And nothing in the mock fleet models an ambiguous/multi-cause failure today, every outcome anywhere in the fleet is a single fixed `{rc, msg}` pair, this is genuinely greenfield and the one piece of new mock-building every 200-series book needs.

**Ambiguous-failure mechanic (applies to every book below)**: fixed, deterministic root cause per exercise, matching this whole series' "every command tested, exact transcript reproducible in print" discipline, but the *symptom* is genuinely ambiguous, one complaint with 2-3 plausible causes, the reader checks multiple real screens to differentiate them, at least one is a red herring, one is the smoking gun.

- [ ] **201 — Orientation**: one small Differential-Diagnosis vignette per platform (z/OS, IBM i, z/VM, z/TPF), deliberately shallow, a 2-screen cross-reference and one red herring each. Teaches the Triage → Isolate → Remediate loop the rest of the series exercises.
- [ ] **202 — IBM i**: pure operational-judgment, no security content (IBM i security is fully claimed by Nuts and Bolts). Candidate scenarios: a batch job stuck in a held queue with a non-obvious cause, a subsystem that looks down but isn't, an RPG program producing wrong output only under certain input conditions. Light touch reframed as operational auditing/forensics (job history/audit trail review, not live-config scanning) rather than security tooling, to avoid overlap; whether this needs new mock content (a simulated audit journal) or can extend existing `WRKACTJOB`/job-log data is a call for this book's own task-list pass.
- [ ] **203 — z/OS**: operational-judgment plus the already-built, book-unclaimed Dataset Recon Scanner (`public/js/recon.js` lines 269-375, flags sensitive-looking dataset names via `LISTCAT`) as the light security touch, explicitly distinct from Nuts and Bolts' RACF-class auditing. Needs at least one job in `PGM_OUTCOMES`/`JCL_MEMBERS` (`mock-lpar.js`) whose failure isn't explained by the RC alone, requiring a second real cross-reference (a dataset listing or storage condition).
- [ ] **204 — z/VM & z/TPF**: paired, mirrors Book 4's pairing. z/VM half: CP-layer resource troubleshooting (a LINK/IPL failure with 2-3 plausible causes, storage, DASD extent, a competing LINK, via `QUERY DASD`/`QUERY VIRTUAL`). z/TPF half: the already-built, book-unclaimed security tools panel (`public/js/tpf-security.js` — ECB Enumerator, Privilege Scanner, Entry Point Prober, Pool Monitor, plus `tpf-tour.js`'s guided tour) becomes the primary content, reframed as Resource Containment & System Stability rather than recycled security tooling. Lightest new-build lift of the series, mostly exercise design against tools that already exist, not new mock code.
- [ ] **205 — Capstone**: cross-platform incident correlating symptoms across all four mocks, mirrors Book 5's role but for diagnosis instead of a scripted happy-path chain, reusing the real multi-session-tabs client mechanic Book 5 already proved out. Biggest single build task of the series, by design, same as Book 5 was for the 100 series. No shared datastore between mocks, same no-integration discipline as the token chain.

Each book above gets its own detailed task list (specific mock code, specific files) logged here once that book's scoping pass happens, following the same pattern Book 4 and Book 5 prep used. No prose gets written for any 200-series book until its task list is closed and verified live.

### Mainframe 201 (Orientation) prep

Four Differential-Diagnosis vignettes, one per platform, each a fixed deterministic root cause behind a genuinely ambiguous symptom, one red herring, one real cross-reference. Scoped 2026-08-21.

- [x] **z/OS — "NIGHTRUN reports a warning, is it a problem?"**: a new job, `NIGHTRUN` (`jcl/programs/nightrun.jcl`, `CATLGCHK` registered in `PGM_OUTCOMES`), completes with RC=04 (real MVS "completed with warnings" convention), step 2's `IEF285I` message names a dataset-already-cataloged condition. Red herring needed no new mock code: the already-seeded `NIGHTBAK` job sits right next to it in the SDSF job list, similar enough a name to genuinely grab the wrong one first, confirmed live in the actual job list output. Verified live end to end: `SUBMIT NIGHTRUN` → ISPF → M → ST → `S NIGHTRUN` shows STEP1 clean (COND CODE 0000), STEP2 at COND CODE 0004 with the exact cataloged-dataset message, `MAXIMUM CONDITION CODE 0004` overall.
- [x] **IBM i — "Is WRKACTJOB lying to you?"**: **zero new mock code needed**, `BCHJOBS` in `mock-as400.js` already has exactly this scenario. `MTHEND` (`*JOBQ`, submitted 07/17 06:00:00) sits right behind `PAYRPT` (`*ACTIVE`, submitted the night before, 07/16 22:00:03) in the same batch job list. Red herring confirmed live: `WRKSBS` shows `QBATCH ACTIVE`, genuinely fine, not the cause. Real cause confirmed live: `WRKBCHJOB`'s list itself shows `MTHEND` waiting behind an already-running job, not a crash or a down subsystem. Pure exercise-design task from here, no build work.
- [x] **z/VM — "The LINK failed, but why?"**: `LINK` to vdev 193 always fails with `HCPLNM097E DEVICE 193 ALREADY LINKED`, in `simulateLink()`. Red herring confirmed live: two attempts with two different, deliberately-wrong-looking passwords produce the exact same message, proving password isn't the variable, undercutting the natural Book 4-callback assumption. Real cause confirmed live: `Q DASD` (extended in `simulateCPQuery()`) shows `DASD 193 ... LINKED TO OPERATOR`, the real conflict. `OPERATOR` reuses the already-seeded logged-on user from `Q NAMES`, no new user needed.
- [x] **z/TPF — "The transaction feels slow"**: **zero new mock code needed**, `POOL_TABLE` in `mock-tpf.js` already seeds `IPOOL` at 95% and `XPOOL` at 97%, and `cmdZshowPools()` already emits the warning. Verified live: `ZSHOW S` shows `SYSTEM HEALTH: NORMAL`, CPU at a mild 38%, the red herring, looks fine. `ZSHOW T` shows `QUEUED: 12`, the ambiguous symptom, elevated but unexplained on its own. `ZSHOW P` is the smoking gun, both pools flagged `***` and `ZTPF303W 2 POOL(S) ABOVE 90% CAPACITY  TRANSACTION REJECTION POSSIBLE`.

All four platform vignettes for Book 201 built and verified live (three needed real mock code, z/OS's `NIGHTRUN` and z/VM's vdev-193 conflict; IBM i and z/TPF needed zero new code, both already had this exact shape of scenario sitting unused). `webterm-3270-saas` synced. 201 drafting can start.

### Mainframe 202 (IBM i) prep

Deep operational-judgment book on IBM i, no security content, that territory is fully claimed by *WebTerm/3270: The Nuts and Bolts Guide*. Three scenarios, genuinely distinct from Book 201's IBM i vignette (the `MTHEND`/`PAYRPT` queue-position case), resolving the plan's open "audit journal or extend existing data" question in favor of the latter, real content already existed. Scoped 2026-08-22.

- [x] **"The report that never printed"**: **zero new mock code needed.** `SPLFILES` in `mock-as400.js` already has `MTHENDRPT` (spooled file 000101) sitting at `*HLD` status, tied to job `MTHEND/QPGMR/048805`, the exact same job Book 201 used as red-herring texture in its `WRKBCHJOB` list. A deliberate callback: the reader who remembers `MTHEND` from Book 201 finds its actual month-end output here, held, not failed, not lost, deliberately withheld pending release. Verified live: `WRKSPLF` shows `MTHENDRPT *HLD`, `WRKOUTQ` shows its target queue `QPRINT` genuinely `RELEASED`, the red herring ruled out cleanly.
- [x] **"What the job log actually says"**: **zero new mock code needed.** `SPLFILES` already has `QPJOBLOG` (spooled file 000098), a real job log for job `RPTPRINT/JSMITH/048790`, three lines: start, a real `CPF1124` message, completion. This is the plan's "operational auditing/forensics, review what already happened" light touch, realized directly, no new audit-journal architecture needed. Verified live via `WRKSPLF`.
- [x] **"MSGW: the job that's neither running nor dead"**: a new `ACTJOBS` entry (`MTHCLOSE`, `QBATCH`, status `MSGW`) and one more entry in `seedMessages()` explaining what it's waiting on. Verified live: `WRKACTJOB` shows `MTHCLOSE ... MSGW`; `DSPMSG` shows `Prior period totals unbalanced. Reply Y or N.`, sized to fit the real 78-column message-list truncation (an earlier, longer draft of the message got cut off mid-sentence, caught by testing the actual rendered width, not just reading the source).

All three IBM i scenarios verified live. 202 drafting can start.

### Mainframe 203 (z/OS) prep

Deep operational-judgment book on z/OS, plus a first, deliberately light security touch, the already-built, book-unclaimed Dataset Recon Scanner (`public/js/recon.js` lines 269-375), explicitly "everyday hygiene," not RACF-class auditing. Scoped 2026-08-23.

- [x] **LISTCAT LEVEL(prefix) support**: genuinely missing from the mock, confirmed by grep, the Dataset Recon Scanner's client-side tool issues real `LISTCAT LEVEL(prefix)` TSO commands but the server had no handler for them, they fell through to `IKJ56500I COMMAND NOT FOUND`. New `CATALOG` table in `mock-lpar.js` (dataset names per prefix, matching `recon.js`'s existing `_DATASET_PREFIXES` list: SYS1, SYS2, IBMUSER, ADMIN, PROD, PAYROLL, FINANCE, HR, SECURITY) plus a `tryListcat()` command handler wired into both the `ready`/`readyOutput` and `tsoCmd` dispatch points, matching the existing `trySubmit()` pattern. Seeded names include real flagged hits (`SYS1.PARMLIB`, `IBMUSER.PRIVATE.KEYS`, `PROD.NIGHTLY.OUTPUT` — a deliberate callback to Book 201's `CATLGCHK` job message, `PROD.APPL.LOADLIB`, `PAYROLL.MASTER.FILE`, `HR.EMPLOYEE.SSN.FILE`) and one deliberate non-hit (`SECURITY.AUDIT.LOG`, proving the scanner flags real sensitive keywords, not just an ominous-sounding high-level qualifier). Verified live over real TN3270E: `LISTCAT LEVEL(PAYROLL)` returns `PAYROLL.MASTER.FILE`, `LISTCAT LEVEL(SECURITY)` returns `SECURITY.AUDIT.LOG` with no flag-worthy hit, both exactly as designed.
- [x] **"The dataset that wasn't actually missing"**: a new job, `PAYVER` (`jcl/programs/payver.jcl`, `PAYVER` registered in `PGM_OUTCOMES`), ends RC=8 with a real z/OS message, `IEF212I PAYVER STEP1 PAYIN - DATA SET NOT FOUND`. Ambiguous symptom: reads like the payroll master file itself may have been lost, a real "call everyone" panic. Real cross-reference (not a JCL read this time, a genuine second screen): `LISTCAT LEVEL(PAYROLL)` shows `PAYROLL.MASTER.FILE` sitting right there in the catalog, fine, ruling out data loss. The job's own `PAYIN` DD actually points at `PAYROLL.MASTER.CURRENT`, a name that stopped existing when the file got renamed during a cleanup and this job's JCL never got updated, a coordination gap, not a data-loss incident. Verified live: `SUBMIT PAYVER` → SDSF → `ST` → `S PAYVER` shows `COND CODE 0008` and the exact `IEF212I` message. Caught and fixed a real rendering bug along the way: `screenSdsfDetail()` always hardcoded an `IEF285I` wrapper in front of a step's message, which double-nested `PAYVER`'s own real message ID (`IEF285I   IEF212I ...`); fixed by only adding the generic wrapper when the message doesn't already carry its own real message ID.

Both pieces verified live. `webterm-3270-saas` synced. 203 drafting can start.

### Mainframe 204 (z/VM & z/TPF) prep

Paired book, mirrors Book 4's pairing. z/VM half needs genuine new mock code (confirmed: `simulateLink()` had no failure path besides Book 201's already-claimed vdev 193, `IPL` had no failure path at all, `Q DASD`/`Q VIRTUAL` had no per-user or multi-device state). z/TPF half needs **zero new mock code**, confirmed by direct comparison of `public/js/tpf-security.js`'s four tools (ECB Enumerator, Privilege Scanner, Entry Point Prober, Pool Monitor) against `mock-tpf.js`'s dispatch — every command they issue (`ZSHOW E/S/P`, `ZSTOP,RPRT`, `ZEND CHECK`, `ZTEST ENTRY,<name>`) already has a real, working handler. Scoped 2026-08-26.

- [x] **z/VM — "The device that was already yours"**: a new vdev, `294`, genuinely different failure shape from Book 201's vdev 193. `LINK ... 294 ...` now rejects with `HCPLNM090E DEVICE 294 ALREADY DEFINED` (new branch in `simulateLink()`), a real, different CP condition — the *requester's own* virtual device address is already occupied, nothing to do with the remote owner's disk at all. `Q DASD` (extended with a 4th line) shows the remote disk, `PRODWK294`, sitting fine and unlinked, ruling out the Book 201-style "someone else already has it" assumption. The real cause only shows up in `QUERY VIRTUAL`, extended to become the mock's first caller-aware query (took a `userid` param through `simulateCPQuery()`, threaded from the existing `cp` case's call site), listing the requester's own already-defined devices, including `294` as a pre-existing scratch disk. Verified live over real TN3270E: `LINK PRODUSR 294 294 MR` fails with the new message, `Q DASD` shows `294` fine, `Q VIRTUAL` shows the real cause, `Q VIRTUAL` from two different logged-on userids correctly shows each one's own userid on the 191/294 lines.
- [x] **z/TPF — "Idle isn't dead"**: zero new mock code. `ECB_TABLE` already seeds two `IDLE`, zero-transaction entries, `SCHD` and `UPGD`, real data nobody had built an exercise around yet. Reader runs the ECB Enumerator (`ZSHOW E`) first (populating the full 15-entry directory, which matters — the Entry Point Prober's own hardcoded fallback target list in `tpf-security.js` only covers 10 of the 15 ECBs and omits both `SCHD` and `UPGD`, so probing either without enumerating first would silently fail against the fallback list), then the Entry Point Prober (`ZTEST ENTRY,SCHD` / `ZTEST ENTRY,UPGD`) to confirm both respond immediately despite zero live traffic, real ECBs, correctly idle by design, not crashed. Verified live.
- [x] **z/TPF — "Containment, not a crash"**: zero new mock code, same `POOL_TABLE` Book 201 already used (`IPOOL` 95%, `XPOOL` 97%), reframed for an architectural angle Book 201 didn't cover, why a high-throughput platform is built to reject or throttle work as pools approach exhaustion rather than let a runaway condition take the whole system down, `ZTPF303W` read as a protective feature, not a symptom. Privilege Scanner gets a short section reframing the OPER/SYSOP/SYSPROG boundary Book 104 already taught manually as something a script can now check automatically, deliberately not re-teaching that boundary from scratch. Verified live.

All three pieces verified live. `webterm-3270-saas` synced. 204 drafting can start.
